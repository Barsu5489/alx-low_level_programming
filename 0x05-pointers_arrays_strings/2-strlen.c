#include "main.h"

/**
 * _strlen - return length of s string
 * @s: paremeter
 * Return: length of a string
 */

int _strlen(char *s)
{
	int l = 0;

	for (l = 0; l <= *(s + l); l++)
	{
	return (l);
	}
}
